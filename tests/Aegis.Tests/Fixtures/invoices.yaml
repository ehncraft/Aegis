resource: invoices
name: invoice-policy

actions:
  view:
    allow:
      roles:
        - Finance

  approve:
    allow:
      when: principal.department == resource.department
    forbid:
      roles:
        - Suspended
